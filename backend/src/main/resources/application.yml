spring:
  datasource:
    # Oracle Cloud Free(Always Free Autonomous Database)용 지갑 없는 TLS 전용 접속 문자열.
    # OCI 콘솔 → Autonomous Database 상세 페이지 → Database connection에서 인증 방식을
    # "Mutual TLS (mTLS)"가 아니라 "TLS"로 선택해야 이 형태의 URL(지갑 zip 불필요)을 쓸 수 있다.
    # 거기서 복사한 접속 문자열을 SPRING_DATASOURCE_URL 환경변수에 그대로 넣으면 된다.
    # 예시(실제 host/service_name은 콘솔에서 확인한 값으로 교체):
    #   jdbc:oracle:thin:@(description=(retry_count=20)(retry_delay=3)(address=(protocol=tcps)
    #   (port=1521)(host=adb.ap-chuncheon-1.oraclecloud.com))(connect_data=(service_name=
    #   xxxxxxxxxxxxx_promptune_medium.adb.oraclecloud.com))(security=(ssl_server_dn_match=yes)))
    url: ${SPRING_DATASOURCE_URL:jdbc:oracle:thin:@REPLACE_WITH_OCI_CONSOLE_TLS_CONNECTION_STRING}
    username: ${SPRING_DATASOURCE_USERNAME:ADMIN}
    password: ${SPRING_DATASOURCE_PASSWORD:promptune}
    driver-class-name: oracle.jdbc.OracleDriver
    hikari:
      # Always Free Autonomous Database는 ECPU가 넉넉하지 않아 동시 세션 수가 제한적이다.
      # Spring Boot 기본값(10)보다 낮춰서 커넥션 풀이 DB 쪽 세션 한도를 넘지 않게 한다.
      maximum-pool-size: 5
  servlet:
    multipart:
      max-file-size: 25MB
      max-request-size: 25MB
  jpa:
    hibernate:
      ddl-auto: validate      # 스키마는 Flyway가 관리
    properties:
      hibernate.format_sql: true
      hibernate.dialect: org.hibernate.dialect.OracleDialect
  flyway:
    enabled: true
    baseline-on-migrate: true
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.security.oauth2.client.servlet.OAuth2ClientAutoConfiguration
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID:dummy-client-id}
            client-secret: ${GOOGLE_CLIENT_SECRET:dummy-client-secret}
            scope: [profile, email]
          naver:
            client-id: ${NAVER_CLIENT_ID:dummy-client-id}
            client-secret: ${NAVER_CLIENT_SECRET:dummy-client-secret}
            client-name: Naver
            authorization-grant-type: authorization_code
            redirect-uri: "${OAUTH_REDIRECT_BASE:http://localhost:8080}/login/oauth2/code/naver"
            scope: [name, email]
          kakao:
            client-id: ${KAKAO_CLIENT_ID:dummy-client-id}
            client-secret: ${KAKAO_CLIENT_SECRET:dummy-client-secret}
            client-name: Kakao
            authorization-grant-type: authorization_code
            redirect-uri: "${OAUTH_REDIRECT_BASE:http://localhost:8080}/login/oauth2/code/kakao"
            client-authentication-method: client_secret_post
            scope: [profile_nickname, account_email]
        provider:
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user-name-attribute: response
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id


ai:
  service:
    url: ${AI_SERVICE_URL:http://localhost:8000}
  mock:
    # true면 AiServiceClient가 실제 ai-service(AI_SERVICE_URL)를 호출하지 않고
    # mock-scenarios.json에 준비된 시나리오로 즉시 응답한다.
    # promptune-portfolio(데모/목업)의 Render 백엔드 서비스에서만
    # AI_MOCK_ENABLED=true로 켤 것 - 실제 운영(promptune)에는 절대 켜지 말 것.
    enabled: ${AI_MOCK_ENABLED:false}

app:
  aws:
    region: ${AWS_REGION:ap-northeast-2}
    s3:
      documents-bucket: ${AWS_S3_DOCUMENTS_BUCKET:promptune-document}

jwt:
  secret: ${JWT_SECRET:promptune-dev-secret-key-change-in-production-please}
  expiration-ms: 86400000

server:
  port: 8080
  # Nginx가 앞단에서 TLS를 종료하고 내부(http)로 프록시하기 때문에, Spring이
  # X-Forwarded-Proto/Host 헤더를 신뢰해서 https 기준으로 리다이렉트 URL을 만들도록 설정.
  # 이게 없으면 구글 로그인(redirect-uri를 {baseUrl}로 자동 생성)이 http://로 잘못 만들어져서
  # "redirect_uri_mismatch" 에러가 남.
  forward-headers-strategy: framework


microsoft:
  client-id: ${MICROSOFT_CLIENT_ID:}
  client-secret: ${MICROSOFT_CLIENT_SECRET:}
  tenant: ${MICROSOFT_TENANT:common}
  redirect-uri: ${MICROSOFT_REDIRECT_URI:http://localhost:8080/api/integrations/microsoft/callback}
  frontend-url: ${MICROSOFT_FRONTEND_URL:http://localhost:3000}
  token-key: ${MICROSOFT_TOKEN_KEY:}

# 2026-08-27: MS 연동 콜백에서 "Invalid CORS request"가 노출된 사례 진단용.
# SecurityConfig의 allowedMethods를 GET/POST 둘 다로 넓혔지만, 혹시 실제
# 원인이 메서드가 아니라 Origin 값 자체의 불일치(예: "null"이 아닌 다른 값)라면
# 이 로그가 다음 재현 시 Spring이 실제로 받은 Origin/메서드를 그대로 보여준다 -
# 동작에는 영향 없음(순수 로그 레벨 조정).
logging:
  level:
    org.springframework.web.cors: DEBUG
